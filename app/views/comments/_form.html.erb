<%= form_for @comment do |f| %>
    <p>
    <%= f.label :input %>
    <%= f.text_field :input %>
  </p>
  <%= f.submit %>
<% end %>


<!-- comment has:

id
input => form
name = @current_user.name
blog_id = params["id"]
user_id = @current_user.id -->
