<h1>Blog</h1>

<div class="container">
  <% @all_blogs.each do |blog| %>

  <div class="section">
    <h4><%= link_to(blog.title, "/blog/#{blog.id}") %></h4>
  </div>

  <div class="divider"></div>

  <% end %>
</div>
